import { Body } from '../../component/body';
import { Dashboard } from '../../features/dashboard';

export const Component = () => {
  return (
    <Body className="p-0">
      <Dashboard />
    </Body>
  );
};
